---
interface Props {
  label: string;
  href?: string;
  type?: "button" | "submit" | "reset";
  variant?: "primary" | "secondary";
  size?: "small" | "medium" | "large";
  blank?: boolean;
}

const {
  label,
  href,
  type = "button",
  variant = "primary",
  size = "medium",
  blank = false,
} = Astro.props;

const baseClasses =
  "inline-block rounded-md font-medium transition-transform transform hover:scale-105 border border-olive-900";
const sizeClasses =
  size === "small"
    ? "px-4 py-2 text-sm"
    : size === "large"
      ? "px-8 py-4 text-lg"
      : "px-6 py-3";
const variantClasses =
  variant === "secondary"
    ? "bg-secondary text-secondary hover:bg-secondary-hover"
    : "bg-primary text-white hover:bg-primary-hover";
const classes = `${baseClasses} ${sizeClasses} ${variantClasses}`;
---

{
  href ? (
    <a
      href={href}
      target={blank ? "_blank" : "_self"}
      rel={blank ? "noopener noreferrer" : ""}
      class={classes + " shadow-button"}
    >
      {label}
    </a>
  ) : (
    <button type={type} class={classes + " shadow-button"}>
      {label}
    </button>
  )
}

<style>
  .bg-primary {
    background-color: #5a6b46;
  }
  .bg-secondary {
    background-color: #f5f2e9;
  }
  .hover\:bg-primary-hover:hover {
    background-color: #4a5a3a;
  }
  .hover\:bg-secondary-hover:hover {
    background-color: #e8e3d5;
  }
  .text-primary {
    color: #f5f2e9;
  }
  .text-secondary {
    color: #5a6b46;
  }
  .shadow-button {
    box-shadow: 4px 4px 0px rgba(74, 74, 58, 0.7);
  }
</style>
